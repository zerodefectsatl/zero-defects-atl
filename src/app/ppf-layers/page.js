'use client'

import PPFScene from '@/components/PPFScene'

export default function PPFLayersPage() {
  return (
    <main style={{ background: '#0a0e16', width: '100vw', height: '100dvh', overflow: 'hidden' }}>
      <PPFScene mode="full" />
    </main>
  )
}
